@props(['campo', 'rotulo', 'opcional' => false])
<fieldset wire:key="tda-field-{{ $campo }}" class="mb-6"><legend class="mb-3 font-semibold text-gray-800">{{ $rotulo }} @unless($opcional)<span class="text-red-600">*</span>@endunless</legend><div class="grid grid-cols-2 gap-3 sm:grid-cols-4">@foreach(['segunda'=>'Segunda','terca'=>'Terça','quarta'=>'Quarta','quinta'=>'Quinta','sexta'=>'Sexta','sabado'=>'Sábado','domingo'=>'Domingo'] as $valor=>$dia)<label wire:key="{{ $campo }}-{{ $valor }}" class="flex items-center gap-2 rounded-lg border p-3"><input type="checkbox" name="{{ $campo }}[]" wire:model.live="{{ $campo }}" value="{{ $valor }}" class="rounded text-red-700 focus:ring-red-600"><span class="text-sm">{{ $dia }}</span></label>@endforeach</div>@error($campo)<p class="mt-2 text-sm text-red-600">{{ $message }}</p>@enderror</fieldset>
